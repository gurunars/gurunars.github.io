import * as React from 'react';



const {{name}} = ({ 
    title,
    onClick
} : {
    title: string,
    onClick: () => void
}) => 
<button 
    style=\{{
        cursor: 'pointer'
    }} 
    onClick={onClick}
>
    {title}
</button>;

export default {{name}};
